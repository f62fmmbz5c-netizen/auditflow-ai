[project]
name = "auditfleet-ai"
version = "0.1.0"
description = "Evidence-backed multi-agent audit control system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
